Transcribe ALL visible text in this image as OCR, preserving the visual reading order.
Return clean Markdown only. Do not wrap the answer in a code block.

Markdown rules:
- Use # / ## / ### headings for prominent titles and section headers.
- Use bullet lists for navigation items, feature cards, FAQ links, and short grouped items.
- Use Markdown tables only when the source has table-like rows or columns.
- Keep paragraph breaks between separate blocks.
- Preserve line order from top to bottom and left to right as much as possible.
- Do not summarize, translate, correct spelling, or invent missing text.
- If text is uncertain, transcribe the most likely visible text without commentary.
